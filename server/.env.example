DATABASE_URL=postgresql://localhost/cardkit
NODE_ENV=development
PORT=3000
# Lock CORS to your domain in production, e.g. https://cardkit.example.com
ALLOWED_ORIGIN=http://localhost:3000
# Set in Render/Railway dashboard — never commit the real key
ANTHROPIC_API_KEY=
