# Sheets service config
SHEETS_SERVICE_PORT=5050
ALLOWED_ORIGIN=http://localhost:3000

# Google auth — these must match the gsheets module's expected env vars
GOOGLE_AUTH_TYPE=service_account
GOOGLE_CREDENTIALS_FILE=/path/to/service-account.json
# GOOGLE_TOKEN_FILE=token.json  # only needed for oauth type
